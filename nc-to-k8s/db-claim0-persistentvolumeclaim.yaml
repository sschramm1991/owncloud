apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: db-claim0-pvc
spec:
  storageClassName: "local-storage"
  volumeName: db-claim0
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi
