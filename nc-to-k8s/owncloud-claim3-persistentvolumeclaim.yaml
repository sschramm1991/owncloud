apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: owncloud-claim3-pvc
spec:
  storageClassName: "local-storage"
  volumeName: owncloud-claim3
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi