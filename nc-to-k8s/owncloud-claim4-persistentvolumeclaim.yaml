apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: owncloud-claim4-pvc
spec:
  storageClassName: "local-storage"
  volumeName: owncloud-claim4
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi