apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: owncloud-claim1-pvc
spec:
  storageClassName: "local-storage"
  volumeName: owncloud-claim1
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi