apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: owncloud-claim0-pvc
spec:
  storageClassName: "local-storage"
  volumeName: owncloud-claim0
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 3Gi